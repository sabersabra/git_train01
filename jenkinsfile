pipeline {
    agent any
    environment {
        imageName = "sabersabra/hello-nginx"
    }
    stages {
        stage("Prepare"){
            steps {
                echo "Hello World"
            }
        
        }
        stage("Check version"){
            steps {
                sh "docker --version"
            }
        }
    }
}