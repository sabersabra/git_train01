node {
    stage("Prepare"){
        echo "Hello World"
    }
    stage("Check version"){
        sh "docker --version"
    }
    stage("Check git vesion"){
        sh "git --version"
    }
    stage("Time Stamp"){
        echo "$date"
    }
}